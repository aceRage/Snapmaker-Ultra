# SDD ledger — plan: docs/superpowers/plans/2026-08-31-paint-depth.md

Spec: docs/superpowers/specs/2026-08-31-paint-depth-design.md (binding; user decisions §User decisions)
Worktree: C:\Dev\SnapmakerOrcaNext · branch feat/paint-depth · BASE = plan commit

## Pre-flight notes
- T1 owns worktree build setup (no build/ yet; mirror supports-worktree config vs main deps).
- T1/T2 seam: T1's paint_depth_band_mm helper is T2's input; T2 consumes config via the new options directly (old key legacy-only).
- Bounded-by-default = OUTPUT CHANGES for painted models by design (user decision); UNPAINTED models must stay byte-identical (T4's parity check is the hard gate).
- [paintdepth] new test tag; chameleon suite must stay green (shared files possible in PrintConfig/Tab).
- Known upstream caveat #6892 (thin parts) tracked to T2/T4.

## Progress
- T1 DONE ed726d71e9: worktree build configured (VS17/x64 vs main deps, BUILD_TESTS=ON, wrapper bats in scratchpad); paint_depth_mode [unlimited|walls|millimeters] default walls / paint_depth_walls 3 / paint_depth_mm 1.5; interlocking default 0.3; handle_legacy nonzero mmu_segmented_region_max_width → millimeters mode (old key legacy-parse-only, REMOVED from Tab UI); paint_depth_band_mm helper RED→GREEN. [paintdepth] 4/10; [chameleon] 133/605 unchanged; ALL_BUILD exit 0.
- T2 DONE: multi_material_segmentation_by_painting now derives its clamp band via paint_depth_band_mm from every printing region's flow (max across regions); interlocking_depth explicitly zeroed for pdmUnlimited (Task 1's 0.3 default would otherwise leak a clamp into "legacy" mode); whole-layer short-circuit (:2149-2151) needed no code change — already flows through the same cut_segmented_layers gate, confirmed by a real end-to-end test that reproduces has_layer_only_one_color directly. New tests/libslic3r/test_paint_depth_clamp.cpp: 4 E2E [paintdepth] cases (real painted mesh -> sliced PrintObject -> inspected applied region geometry), real TDD RED confirmed via git-stash bisection. #6892 thin-feature fixture not attempted (documented GUI-round note instead). [paintdepth] 8/40 stable; [chameleon] 133/605 unchanged; ALL_BUILD exit 0. Report: task-2-report.md.
- T2 DONE 89d3f15d44: clamp wired (band from paint_depth_band_mm, all-regions-max width per fuzzy-skin precedent; whole-layer ring routed through the cut path); beam-interlock exclusion preserved. Tests are END-TO-END (painted mesh → sliced PrintObject → claimed geometry inspected — the unit level escapes the CLI painted-3mf limitation!): [paintdepth] 8/40 stable; [chameleon] 133/605; ALL_BUILD exit 0. #6892 thin-feature → GUI-round note. Noted pre-existing outer_wall_line_width/Flow::spacing config gap (worked around, unfixed).
- T3 DONE: Stage 2 vertical bleed. (a) Z-interface solid skin — PrintObject::has_bounded_paint_depth() (is_mm_painted() && paint_depth_mode != unlimited) ORed into interface_shells' effective value at both its read sites (detect_surfaces_type(), discover_vertical_shells()); documented trade: whole-object flip (every region boundary gets solid skin), not a scoped paint-only reclassification, since LayerRegion::slices carries no "why did this region differ" provenance. (b) paint_infill_override coBool default true, threaded through BOTH generate_print_object_regions() (fresh regions) AND verify_update_print_object_regions() (the fast region-reuse path that runs on every apply() — missing this one would make the toggle a silent no-op); gated on bounded mode to match the UI greying (flagged as a judgment call). New tests/libslic3r/test_paint_depth_clamp.cpp cases: 3 region-config-level for the override, 2 end-to-end (print.process(), not just slice() — detect_surfaces_type() only runs at the private prepare_infill() step) for the z-interface, using a new two-stacked-cube fixture (real color Z-boundary, no custom mesh needed). Real TDD RED confirmed via targeted git-stash bisection on both items. [paintdepth] 13/56 stable; [chameleon] 133/605 unchanged; [MixedFilament] 178/1/2(expected) identical to T2's baseline; ALL_BUILD exit 0. Report: task-3-report.md.
- T3 DONE 7579272f9d: z-interface solid skin via has_bounded_paint_depth() OR-ed into interface_shells at its two read sites (region boundaries carry no color provenance — whole-object flip accepted, trade documented; adds skin at non-paint boundaries too in bounded painted objects); paint_infill_override coBool default true (false ⇒ base-color SPARSE only; inert in unlimited — judgment call, reversible). [paintdepth] 13/56 incl. a REAL end-to-end z-interface test through Print::process(); [chameleon] 133/605; ALL_BUILD exit 0.
- T4 DONE: spike/verify_paintdepth.sh (17 checks, modeled on verify_chameleon.sh's normalize()/sliver-strip pipeline): (a) UNPAINTED-fixture inertness vs a FROZEN pre-feature baseline gcode (spike/out/paintdepth_baseline.gcode) recorded from a one-time throwaway build of Snapmaker_Orca_app_gui at the merge-base commit c345859f55 (confirmed byte-identical in code to ed726d71e9~1) in a temp `git worktree add`, then the worktree/build tree fully removed; (b) determinism (2 consecutive slices, normalized-identical); (c) config surface: defaults resolve (walls/3/1.5/interlock 0.3), legacy nonzero mmu_segmented_region_max_width -> millimeters mode + value carried verbatim, legacy zero -> stays walls (NOT unlimited, per Task 1's deliberate bounded-by-default-on-reslice decision) — all asserted via resolved CONFIG_BLOCK dump values + --debug=3 loader log evidence. Painted-fixture CLI checks deliberately skipped (Task 2/3's [paintdepth] unit harness already covers painted behavior end-to-end; GUI-exported painted 3mfs are a known CLI segfault). verify_paintdepth.sh 17/17 x2; [paintdepth] 13/56 x2; [chameleon] 133/605 x2. No C++ touched. GUI handoff checklist (7 items incl. #6892 thin-feature caveat) in task-4-report.md. Report: task-4-report.md.
- T4 DONE c7b2a59292: spike/verify_paintdepth.sh 17 checks (unpainted byte-parity vs frozen pre-feature baseline from temp merge-base worktree, determinism, config defaults + legacy mapping) 17/17 ×2.
- FINAL REVIEW: FIX FIRST — 0C/3I/5m. F1 interlock semantics drifted (interlocking_depth used AS the even-layer band, Prusa interlocking_cut_width DEAD → alternating 1.3/0.3mm depth); F2 legacy flip-flop (old key left nonzero → re-migrates on every diff-preset reload); F3 interface_shells has FOUR read sites (2 missed in PerimeterGenerator).
- FIX WAVE e64a4e154e: F1 Prusa-verbatim restore (WebFetch-verified upstream) + alternation test RED-proven; F2 old-key neutralize + flip-flop pin; F3 all four sites via in-class-initialized bool; F4 mm=0 coherent unlimited; F5 tooltip. [paintdepth] 15/84; [chameleon] 133/605; verify 17/17 ×2.
- RE-REVIEW: CLEAR, all five RESOLVED, no regressions (interlock≥band saturates; no uninitialized read). PAINT DEPTH CODE-COMPLETE at e64a4e154e. AWAITING USER GUI ROUND 1 (checklist in task-4-report.md: dark spot ≤3 walls + base interior, full ring bounded, mode toggles, interlock notch, Stage 2 skin, override=false light sparse, thin-feature #6892 check).

## GUI round 1 (user) + vertical-depth follow-up

User verdict: paint depth "works well in test cases" (lateral clamp validated on a real multicolor model). New symptom: object printed LYING DOWN — base color visible upward through painted top features ("lack of vertical shell covering layers in the top filament").
- TWO PARALLEL INVESTIGATIONS (both code-anchored, agreeing): (a) our clamp is NOT at fault — cut_segmented_layers (:2182) runs BEFORE segmentation_top_and_bottom_layers (:2189), mutates only segmented_regions, and interior top/bottom islands never enter that container (merge at :1838-1881 diffs the lateral claim BY the top/bottom claim). DO NOT reorder. (b) Verdict = WRONG COLOR, not missing solid: our Stage-2 has_bounded_paint_depth→interface_shells OR already forces stTop on the base region under the painted patch, so base-colored SOLID (not sparse) fills the gap; filament is base because frTopSolidInfill uses the region's solid_infill_filament.
- ROOT CAUSE (pre-existing upstream gap, exposed by the feature): segmentation descends exactly top/bottom_shell_LAYERS (:1400/:1420, counts :1210-1211/:1366-1367) and never consults *_shell_THICKNESS, while discover_vertical_shells (:1961/:1990) and discover_horizontal_shells (:4144) use max(layers, mm). Thin layers ⇒ shell deeper than paint claim ⇒ base-colored solid shell layers under the painted skin. Edge: top_shell_layers=0 + thickness>0 ⇒ ZERO claim (gate :1225).
- FIX DISPATCHED: effective shell-layer count = shell generators' own max(layers, mm) math mirrored (incl. variable-layer-height handling) at the count/granularity/gate sites; shell counts taken as MAX over regions (:1361/:1366); *_shell_thickness routed into posSlice invalidation; erosion/break at :1403/:1407 untouched (upstream sliver-bug class). Investigation reports: vertical-depth-investigation.md, shell-coverage-investigation.md.
- FIX LANDED: new effective_shell_layers_by_thickness() helper (MultiMaterialSegmentation.cpp, mirrors discover_vertical_shells/discover_horizontal_shells' max(layers,mm) walk against each layer's real print_z/bottom_z, proof that max(n_layers, thickness-driven-count) == the shell generators' interleaved OR-walk via prefix-set monotonicity) feeds layer_color_stat's top/bottom_shell_layers (now maxed over ALL regions per fix #2, not just wall_filament==color_idx) and the global gate/granularity sizing (conservative min-layer-height upper bound, fixes the layers==0+thickness>0 zero-claim gate bug with no edit to the gate expression itself); top_shell_thickness/bottom_shell_thickness moved into the posSlice invalidation group (posSlice already cascades to posPrepareInfill). cut_segmented_layers ordering and the :1403/:1407 erosion/break untouched (verified via diff re-read, not just assumed). 4 new [paintdepth] E2E tests (thin-layer top-claim-reaches-6-layers RED-proven pre-fix, top_shell_layers=0+thickness>0 nonzero-claim RED-proven, layers-driven-unchanged regression guard, bottom-face mirror) on a new slice_capped_slab() fixture (painted cap on a thin slab, pdmUnlimited to isolate from Stage 1's lateral clamp). [paintdepth] 19/122 (was 15/84); [chameleon] 133/605 unchanged; ALL_BUILD exit 0; verify_paintdepth.sh 17/17 x2 (unpainted parity holds — this path is gated behind is_mm_painted() at the call site, never reached for unpainted objects). Full libslic3r_tests suite (extra, beyond the binding gate): 447 cases, only 2 pre-existing [!shouldfail]-tagged known-bug cases in the unrelated [MixedFilament][batch_remap]/[differential] subsystem, confirmed unrelated. Report: vertical-depth-fix-report.md.
- VERTICAL FIX 41394ce2b4 (effective shell layers honoring *_shell_thickness, max-over-regions, invalidation): 19/122. REVIEW: FIX FIRST 1C/3I — C1 = the "layers==0 gate" premise in MY brief was WRONG (generators gate on n_layers>0 ⇒ NO shell at all, so the fix claimed paint into sparse infill and a test pinned it); I1 off-by-one on walk exhaustion; I2 max-over-regions is object-wide (every layer holds a LayerRegion per PrintRegion); I3 tests lost non-uniform-height coverage.
- FIX WAVE 530e2f52d2: C1 zero-count ⇒ zero claim (test INVERTED); I1 ++m on exhaustion; I2 empty-slices skip; I3 non-uniform fixture + ceil-swap experiment (only the new test goes red — claim now pinned). 22/156.
- RE-REVIEW: ISSUES — N1 (CRITICAL, regression from I2): the empty-slices guard sits ABOVE the per-colour block, so it also zeroes num_regions/extrusion_width/spacing/small_region_threshold for EVERY painted colour (the painted region's slices are empty until apply_mm_segmentation runs AFTER segmentation) ⇒ kills lateral taper + #7104 filter + re-arms an assert (masked in Release). N2 :1536 ungated surface layer; N3 missing bottom-direction zero-shell case. N1/N2/N3 FIX WAVE DISPATCHED.
- CHECK-5 ANSWER (REAL product behavior, not fixture artifact): the per-layer inward erosion taper (~0.87mm/layer, upstream, keeps paint inside walls) means a painted face is full width ONLY at the surface layer; features narrower than ~10mm never receive the deeper claim — so the thickness fix helps WIDE faces only, and small features (eyes/cheeks) keep base-coloured solid under the skin.
- USER DECISION 2026-08-31: bound the taper — painted top/bottom claims keep FULL WIDTH for the solid-shell depth, taper only BELOW that. Fenced as its own reviewed change after N1 lands (same file/function; investigation B ranked this fix #3 = highest risk, revives upstream sliver class #7235 ⇒ needs guards + dedicated tests).
- N1/N2/N3 FIXED 3448111acd: guard rescoped to shell-depth max only (per-colour stats/taper restored); top surface claim gated on shell>0; bottom zero-shell coverage added. ASSERT RUNTIME-PROVEN via the project's per-TU #undef NDEBUG pattern (pre-fix aborted: "out.num_regions > 0" line 1525), reverted before commit. 25/173.
- TAPER BOUND 65d17c964f (user-approved): VERIFIED erosion purpose = perimeter-safety margin on INFERRED claims — the surface layer is appended at offset 0 (what the user painted), every deeper layer must stay one wall stack clear of the contour or the colour boundary lands inside a perimeter band and dimples the exterior (upstream #7104/#7235, both fetched; BBS widened upstream's one-perimeter to wall+spacing). Guard = exposed_surface_part(): the part of a projection more than one wall stack clear of the layer above descends FULL WIDTH (trimmed only by the running outline), unioned with the untouched eroded term; slope criterion is the erosion's own yardstick (layer_height/tan(slope) >= wall+spacing), no invented angle. Rejected: delete erosion / constant one-band / normal-Z facet split / erode-past-shell / snap-to-contour. Bottom gate folded in. 29/259; anti-smear frustum proof (both-offsets-deleted build fails ONLY that test).
- TAPER REVIEW: FIX FIRST 0C/2I. I1 = early-return at :1331-1333 skips the clearance test ⇒ sliver class REVIVES on chamfered/filleted painted tops (45° chamfer @0.1mm ⇒ 0.15-0.55mm base rings vs 0.879mm wall stack; is_perimeter_compatible never merges them, cleanups only catch ~0.0005mm). I2 = anti-smear test's 0.2mm positive probe sits inside the lateral band ⇒ can pass with the vertical path dead. FIX WAVE DISPATCHED.
- CHECK-4 (GUI guidance): flat top + VERTICAL walls is the safe case and proves nothing; a CHAMFERED/filleted painted top is the one to inspect (exterior wall 2-6 layers down).
- CHECK-5 (REAL REMAINING GAP, numbers): lateral clamp does NOT close the shallow-slope band. Normal painted thickness = b·sinθ with b = 0.45 + 2×0.42854 = 1.307mm ⇒ 0.148mm @6.5°, 0.227 @10°, 0.338 @15°, 0.447 @20°, 0.552 @25°, parity with the flat 0.60mm only at ~27.3°. So 6.5°-27° slopes stay thin, with a 4x cliff at the 6.5° threshold; THINNER layers make it worse (threshold →3.2° @0.05mm). Lever = paint_depth_walls 5-6 or paint_depth_mm. Open product question after GUI round 2: raise the default, or make lateral depth slope-aware.
- TAPER FIX WAVE cfe7fae1df: I1 sub-wall-stack base rings absorbed via opening_ex sliver detection in BOTH descent loops (frustum-cap test RED 5/18 pre-fix at the hand-derived (k+0.5)*0.15mm gaps, 18/18 after); I2 anti-smear test switched to pdmMillimeters/0.15 + 0.30mm probe so the lateral band can no longer satisfy it — falsification re-run confirms it still fails with the erosion deleted AND now also fails if the vertical path dies. [paintdepth] 30/277; [chameleon] 133/605; ALL_BUILD 0; verify 17/17 x2.
- GUI ROUND 2 VERDICT (real slice): exterior paint bleed on painted caps + "clearly only 1-2 walls" at paint_depth_walls=3. TWO INVESTIGATIONS (outward-bleed-investigation.md, wall-count-investigation.md) ⇒ four defects, one fix wave.
- BLEED-AND-WALLS FIX WAVE: F1 exterior bleed was OUR regression (65d17c964f full-width term bounded only by a layer outline ⇒ painted claim reached the contour on every sub-surface shell layer ⇒ 0.7-1.0mm ring of exterior side wall); fixed by intersecting with offset_ex(input_expolygons[last_idx], -wall_stack) — CONSTANT one-wall-stack inset measured at the DESCENT layer (correct for downward-narrowing objects, unlike a layer_idx test), interior features untouched, surface layer untouched. I1 absorb DELETED at both sites: F1 makes base_rest one wall stack wide by construction so the absorb's diff is empty, and keeping it would leave it sitting exactly on the Clipper arc-approximation knife-edge, re-appending residue onto the contour — two mechanisms fighting one invariant. Its test keeps fixture+subject with probes flipped. F2 lateral clamp SELF-DISABLED wherever local half-thickness < cut_width (diff_ex(claim,∅)==claim, silent on/off cliff, true across most of an organic model at 4-6mm); new paint_depth_clamp_keep_core() halving ladder with a DOUBLED membership test ⇒ effective band b with t/4 < b <= t/2, base keeps >= half the local cross-section; thick geometry bit-identical; degenerate limit = parts thinner than band/64 keep the no-op (0.022mm @1.44mm band); does NOT stop Voronoi tip wrap-around (annulus keeps within band of ANY boundary — needs a painted-boundary-relative clamp, out of scope, recorded). F3 band = N*s + 2*(ext_w-ext_s) + 0.25*s (1.435675 @0.45/0.1 vs old 1.307080; margin 0.083 -> 0.212mm, 2.5x, parity-independent); 2*wall_loops bead CEILING documented in header+tooltip, not fixed (stock wall_loops=2 ⇒ max 4 painted loops; walls=5 needs wall_loops>=3). F4 interlocking clamped to min(configured, 0.25*perimeter_spacing) = the band's own count-window margin, default 0.3 -> 0.1 (ONLY default changed; walls/mm defaults untouched per user decision), band NOT widened to compensate (proved to convert 3/2 into 4/3); clamp uses MIN spacing across regions. Hand-walked: 3 beads on both parities at 0.1mm AND 0.2mm layers. TDD RED 10 cases/57 assertions (F1 interior-boss guard green in both builds by design); F4 asserts on BAND WIDTH at the exact N-bead optimum, bead counting unreachable from a slice()-only harness. [paintdepth] 36/416 (was 30/277); [chameleon] 133/605 unchanged; ALL_BUILD exit 0; verify_paintdepth.sh 17/17 x2 with unpainted byte-parity intact (script's interlocking default expectation updated 0.3 -> 0.1). Report: bleed-and-walls-fixwave-report.md. NEXT WORK APPROVED: curved-surface gap — F1 is exactly neutral there (all its changes sit inside the `top_exposed_ex non-empty` guard, dead above ~6.5-13°) but CONSTRAINS the fix: any design that means to own the outer perimeter of a sub-surface layer on a curved face conflicts with F1 and would revive the bleed; the >~24° arm (opening_ex(top_ex, small_region_threshold) at :1618/:1690) is untouched and still the live lever; F2 now caps lateral depth at b<=t/2 on thin shells so widening the band there buys nothing.
- SUPERSEDED (kept for history) — PAINT DEPTH CODE-COMPLETE (vertical work) at cfe7fae1df. AWAITING GUI ROUND 2: (1) CHAMFERED painted top — inspect exterior wall 2-6 layers down (flat+vertical-walled cube proves nothing); (2) the lying-down model that showed the original bleed; (3) if features sit on 6.5-27 deg curvature they stay thin by design — try paint_depth_walls 5-6 or mm mode; report which. Open product call after: raise the default vs slope-aware lateral depth.

## GUI round 2 (user) + the normal-thickness reframing

Round 2 findings: (1) mm mode 4-6mm paints OUTWARD (bleed); (2) no depth difference on curved/tops; (3) "3 walls" looks like 1-2 walls. User calibration: "about 3 walls, 2mm to really be useful". USER RUNS THE CLASSIC WALL GENERATOR.
- OUTWARD BLEED = OUR REGRESSION (65d17c964f): pre-taper-bound code inset claims >=0.86mm from the contour unconditionally, so exterior bleed was structurally impossible; full-width descent reaches the contour on flat/chamfered caps, and the I1 absorb made it worse on chamfers. Domes unaffected (>6.5 deg gate). SEPARATE BUG: cut_segmented_layers self-disables (inner offset empty) when local half-thickness < cut_width, so at 4-6mm the clamp no-ops across most of a model and the unclamped partition wraps around thin fins.
- "1-2 WALLS" = LABELLING ARTIFACT, not lost material: at N=3 classic tiles the band with outer loop + gap fill + hole-side loop = 97% painted; annuli always show even loop counts and the middle line is tagged erGapFill. BUT two real classic-only defects: paint_infill_override=false sends the band's GAP FILL to sparse_infill_filament (~37% of an N=3 band goes base colour; Arachne emits no gap fill so we never saw it), and at walls=1 F1's wall_stack inset exceeds band(1), leaving a 0.3mm ring classic prints as NOTHING (void under painted caps). Fix = classic-only band floor at ext_w+ext_s. ALSO: every [paintdepth] test runs Arachne (no test sets wall_generator; suite never asserts on loops).
- FIX WAVE 193d2e0675 (F1 contour inset + I1-absorb removal, F2 thin-geometry graceful clamp, F3 band formula N*s+2(ext_w-ext_s)+0.25s, F4 interlock clamp 0.25*spacing + default 0.3->0.1): 36/416. REVIEW: FIX FIRST 1C/6I — C-1 F2's ladder starts at band/4 with no lower bound ⇒ sub-extrusion claims (0.359 < 0.45 ext width) that Arachne over-extrudes ~64% or drops; I-1 keep_core above the empty guard (perf); I-2 thresholds from the notched band (re-introduces alternation); I-3 F4 clamp applied in mm mode where no bead contract exists; I-4 one-sided assertions (0.112mm upward margin unpinned); I-5 loose base-ring probe; I-6 no downward-narrowing fixture.
- USER REFRAMING (drives Wave B): painted colours should behave like SEPARATE OBJECTS without the tolerance gap — "inward taper and normal projection". Engineering form: a CONSTANT-THICKNESS SHELL measured NORMAL to the painted surface (lateral D/sin(theta), vertical D/cos(theta)) — degenerates correctly at both extremes and covers the 6.5-27 deg dead band. Already true in our impl: painted regions get their own perimeters + their own solid shells, no clearance. FUTURE IDEA (logged, not designed): split painted colours into separate objects in place.
- CURVED-GAP DESIGN = Option N (recommended): the descent ALREADY computes a slope-correct claim (M*h*cos(theta) normal); it is merely gated off above 6.5 deg by exposed_surface_part. Delete that gate, keep F1's inset verbatim, bound the descent by normal depth D instead of shell layers, relax the break. Result at D=1.4357: 1.414/1.387/1.349/1.301mm normal at 10/15/20/25 deg (today 0.249/0.372/0.491/0.607), never below D/sqrt(2); F1 makes it SAFE and auto-suppresses above 58.5 deg. Config: paint_depth_mm becomes the headline = normal thickness (user's 2mm = 2.0, and on classic that needs no wall_loops bump); walls-mode relabelled as "~N wall widths of material normal to the surface" — NOT an Arachne bead promise. Hazards: break placement (else no-op) and granularity widening at :1449-1465 (else TBB double-buffer race). Cost: flat tops claim ~15 layers instead of 6 at 0.1mm (accepted per the fidelity ruling).
- WAVE A DISPATCHED (correctness first): C-1 + I-1..I-6 + classic band floor + gap-fill filament leak + classic-generator test coverage. WAVE B (Option N) follows on a clean base.
- WAVE A LANDED (all 10 items). C-1 ladder floored at min_claim_width = max ext_perimeter_width across regions (plumbed segmentation_by_painting -> cut_segmented_layers -> paint_depth_clamp_keep_core); below it the pre-F2 no-op stands, so a thin fin gets either a >=1-extrusion skin or the whole cross-section, never a sub-bead sliver Arachne over-extrudes 64% or drops. I-1 keep_core moved below an all-claims-empty guard. I-2 ladder step/thresholds now from the UN-NOTCHED cut_width (notch kept only on the full-band core erosion) — kills the parity halving/doubling; consequence: no notch on degraded thin-geometry claims (deliberate, documented). I-3 DECIDED = clamp in WALLS MODE ONLY, via a mode parameter on paint_depth_interlocking_depth_mm (the cap's whole derivation is the walls-mode count window; mm mode's contract is "mm verbatim, flow widths ignored"; the tooltip already said walls; default 0.1 is under the cap so nothing changes by default). I-4 two-sided: band - optimum pinned EXACTLY at 0.25*s, notch pinned subtractive-only, plus a geometric over-probe measured against the SPEC band not the helper's output. I-5 frustum base ring pinned to >= one wall stack (0.8mm negative probe, 0.078mm clearance). I-6 two NEW downward-narrowing fixtures (frustum 22->40 top cap, frustum 40->22 bottom cap) pin input_expolygons[last_idx] vs [layer_idx] in both descent loops. ITEM 8 classic-only band floor at ext_w+ext_s = one wall_stack via new paint_depth_band_classic_floor_mm (fixes N=1 +48% over-extrusion, the wide-outer-wall total-disappearance cliff, and F1's 0.3mm void ring under painted caps — band(1) now EQUALS F1's inset so they meet by construction); NOT unconditional (a floored band(1) gives Arachne 2 beads for "1 wall", pinned by a negative Arachne probe). ITEM 9 gap-fill leak fixed at a NEW shared role->filament rule fill_filament_source(PrintRegionConfig, ExtrusionRole) in Print.hpp/PrintRegion.cpp, used by BOTH of GCode::process_layer's extruder-id lambdas: erGapFill -> wall_filament. It was a bucket-vs-role confusion (gap fill is wall-family, merely stored in fills), and it also settles the standing LayerTools::extruder vs GCode disagreement. ITEM 10 first classic coverage in the suite AND first assertions on extrusions anywhere: classic pinned at 2 loops + >=1 gap fill, Arachne at >2 loops + 0 gap fill, on Print::process()-based fixtures. TDD: RED-1 (tests only + behaviour-preserving extraction) 5 cases/7 assertions red for C-1, I-3, item 8, item 9; RED-2 (I-3 applied + 3 deliberate mutations: F1 inset halved, last_idx->layer_idx, band margin 0.25->0.5 with the test's spec mirror updated) 12 cases/48 assertions red, covering I-2, I-4 (x25), I-5, I-6. GREEN [paintdepth] 43/579 (was 36/416); [chameleon] 133/605 unchanged; full libslic3r_tests 471 cases exit 0; ALL_BUILD exit 0; verify_paintdepth.sh 17/17 x2 with unpainted byte-parity intact. Defaults untouched (walls stays 3). One number deliberately NOT pinned: Arachne emits 6 loops + 4 open paths on a square painted annulus (corner width = band*sqrt(2) adds short beads) — captured as a diagnostic, not asserted. Report: wave-a-report.md. WAVE B unblocked and unimpeded: exposed_surface_part, the descent break, small_region_threshold and the granularity sizing are all untouched; the anti-smear fixture still passes unmodified; slice_painted_frustum() + the new extrusion counters are reusable for T1/T2/T3.
