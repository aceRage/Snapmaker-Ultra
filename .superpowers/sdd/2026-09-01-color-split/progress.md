# SDD ledger — plan: docs/superpowers/plans/2026-09-01-color-split.md
(Colour Split — Split by painted colour)

- 2026-09-01: research phase. Three read-only code audits (paint API, model/plater/slicing plumbing, boolean/OpenVDB/jobs infrastructure) + prior-art search; user's brainstorm notes (C:\Dev\SnapmakerOrcaSlicerFixes\docs\colorsplitting_topics.md) found unsuitable (cap-and-boolean gives zero-volume pieces on flat faces). Findings: docs/superpowers/specs/2026-09-01-color-split-research.md.
- 2026-09-01: user decisions — engine A (exact shells + Manifold), depth mirrors paint-depth, parts in the same object, spike first in the new worktree.
- 2026-09-01: worktree C:\Dev\SnapmakerOrcaNext switched to feat/color-split off origin/main dff2c65eab (paint-depth merge). Build tree in build/ is stale (built from feat/paint-depth); build slot was busy (another session) so no rebuild started.
- 2026-09-01: spec rev 1 → adversarial review (see spec-review.md) → rev 2 committed d6f4e79566. Rev 2 adds: half-thickness clamp d(v)=min(D,t/2) + fold guard + CGAL self-intersection check (C1), pinch-vertex duplication (I1), live 2D cap rule tanθ<h/(3ws) and N_eff cap depth incl. N=0 (I2/I3), new volumes via public add_volume + rotation-safe offset + reset of text/emboss/cut info (I4/I5/I7), empty body handling (I6), job id/timestamp re-check + gizmo guard (I8), paint_infill_override mirror (I9), wall-stack step at horizontal-painted/vertical-neighbour boundaries (I10), sequential Manifold::Split partition + enclosed-island absorption (I11 + C1), depth inputs spelled out (I12), MeshGL64/tolerance/Volume() (M6), ExecutionContext cancel (M7).
- NEXT: user reviews the spec (brainstorming gate) → writing-plans → spike S1–S4 as plan task 1 (needs a rebuild of build/ on the new branch; check the build slot first).
- 2026-09-01: user said "Continue" on the spec review gate → spec rev 2.1 (§3.6 crease rule extended with the painted-side/more-horizontal-neighbour case) and implementation plan docs/superpowers/plans/2026-09-01-color-split.md written (9 tasks; Tasks 1-4 double as the spike with a decision checkpoint in Task 4 step 5). Execution mode (subagent-driven vs inline) pending user choice. Build tree still stale; build slot busy at last check.

## Pre-flight scan (2026-09-01, before Task 1)

| Pair / task | Produces vs consumes | Finding |
|---|---|---|
| T1 → T2 | ColorPatches{surface, facet_state, states} / compute_vertex_depths(const ColorPatches&, normals, D) | consistent |
| T2 → T3 | color_split_normals(its), compute_vertex_depths(...) / build_color_shells calls both | consistent after Ruling 1 (per-state depth call) |
| T3 → T4 | ColorShell{state, capped, mesh}, build_color_shells(patches, depths, params, progress) / partition_by_shells + split_volume_by_paint | consistent |
| T3 ↔ T5 | ShellBuilder internals; T3 tests pin cap/step OFF via no_cap_no_step(), T4 partition tests too | consistent; T4 spike cases use defaults → behaviour changes after T5 by design (numbers re-recorded in T9) |
| T4 S1 boss ↔ T2 depth rule | "whole boss after island absorption" vs d = min(D, t/2) | CONFLICT: with t/2 the boss core stays a body sliver connected to the block → not absorbed; with a full-thickness rule the ruled shell crosses itself (nested shells, Manifold semantics unknown). Ruling 1 below. |
| T3 fold guard area criterion | bottom area < 1e-3 top area | defect: legitimate deep offsets on small spheres collapse to tiny-but-valid triangles and would be halved; Ruling 2 |
| T5 crease Case B | spec rev 2.1 §3.6 | plan text carries both cases; consistent |
| T6 → T8 | apply_color_split(ModelObject&, size_t, ColorSplitResult&&, const ColorSplitSpace&, bool, bool), color_split_space, scale_depths / Plater::split_by_color + ColorSplitJob::finalize | consistent |
| T2 → T8 | color_split_depths(cfg, filaments) / Plater builds filaments from v->get_extruders() | consistent (1-based) |
| T7 | e2e uses ColorSplitParams{} (cap+step on) after T5 | consistent |
| Each task self-consistency | tests vs code | T1 select_patch call flagged "verify overload" (brief carries the real signature) |

Rulings:
- Ruling 1 (depth rule): d(v) = min(D, t(v)/2 − δ), δ = 0.002 mm, computed PER STATE (a vertex's thickness ray is the same, the clamp is universal). Through-painted features (both sides the same state, or a pin/boss) become two half-shells whose bottoms stop δ short of the mid-surface; the 2δ sliver between them is either an enclosed body island (absorbed into the colour, §3.8) or a sub-resolution body sliver the slicer drops. No shell ever crosses the mid-surface, so no nested/inverted shells reach Manifold. — Why: keeps every shell valid by construction and mirrors the 2D Voronoi mid-split; avoids depending on Manifold's undocumented winding semantics. — Cost if wrong: hidden micro-slivers of body colour inside pins (invisible; measured by the S1 boss test at ≤5% volume).
- Ruling 2 (fold guard): the area criterion becomes "bottom area < 1e-6 × top area" (truly collapsed only); the orientation criterion nb·(−nt) ≤ 0 stays. — Cost if wrong: a few more CGAL-check retries.
- Ruling 3 (spec rev 2.2): §3.4 text updated to Ruling 1 in both copies (worktree spec is binding).
- Ruling 4 (models): implementers T1/T2 sonnet, T3–T8 opus; task reviewers sonnet for T1/T2/T6/T7/T9, opus for T3/T4/T5/T8; final whole-branch review fable.
- 2026-09-01: build gate on feat/color-split (libslic3r_tests target) EXIT=0; [paintdepth] 94 cases / 1568 assertions green. BASE for Task 1 = d3a1084b7c. Task 1 dispatched (sonnet).
- Task 1: review clean (spec ✅, quality Approved). ⚠️ trailer verified by controller (present). Minor (deferred): (1) report overstated test_mixed_filament's matcher consistency — no action; (2) test_color_split.cpp:96 still uses Approx → folded into Task 2; (3) assert-only guard on the shared-vertex-pool invariant (ColorSplit.cpp:34) → folded into Task 2 as a ColorSplitError throw; (4) its_merge_vertices never exercised with duplicate vertices — coverage gap for the final review; (5) make_grid_box/paint_by_predicate unexercised until later tasks — expected; (6) test 1 does not assert remainder facets are state 0 → folded into Task 2.
- Task 1: complete (commits d3a1084b7c..0bb0997d9f, review clean)
- BASE for Task 2 = b7b8404be6; Task 2 dispatched (sonnet) with the three folded T1 minors.
- Task 2 implementer reported DONE_WITH_CONCERNS (813a0ed4ad). Ruling 5 (plan defect): Task 1 registered ColorSplit.hpp/.cpp in the libslic3r_cgal source list (CMakeLists.txt:510, "next to MeshBoolean" as the plan said) — that static lib has no OpenCASCADE include path, so NormalUtils.hpp (which includes Model.hpp) cannot be used there and the implementer duplicated the angle-weighted normal algorithm locally. Correction: register ColorSplit in the MAIN libslic3r source list (after PaintDepth.cpp, line ~87) and use NormalUtils::create_normals as the brief specifies; libslic3r links manifold (line 600) and libslic3r_cgal, so Tasks 4/6 also need it there. — Cost if wrong: none (moving a source between the two static libs of the same DLL). Addressed as a pre-review fix by resuming the Task 2 implementer.
- Ruling 6: the brief's plate test expected 0.598 at the top-face vertices of a plain make_cube, but those are corner vertices whose angle-weighted normal is the diagonal bisector, so the ray thickness is 1.2·√3 and d = 1.03723; the implementer's corrected expectation plus a make_grid_box interior-vertex check (0.598) is accepted. — Cost if wrong: none (test contract only).
- Ruling 7 (pre-existing bug, verified by controller): NormalUtils::indice_angle (src/libslic3r/NormalUtils.cpp:50-66) indexes `vertices[i]` by triangle-local position instead of `vertices[indice[i]]`, so create_normals_angle_weighted uses one fixed weight pair for every triangle; ColorSplit is the only caller of AngleWeighted in the tree. Decision: fix the three lookups in NormalUtils.cpp in its own `fix(libslic3r):` commit with a regression test in test_color_split.cpp (cube-corner angle-weighted normal == diagonal bisector); keep the brief's expected values. — Cost if wrong: none elsewhere (no other caller); upstream-visible fix.
- Task 2: pre-review fixes applied by the resumed implementer (Rulings 5-7): commits 813a0ed4ad, e8a6fa11d7 (NormalUtils fix + regression test), 7f48e2b8a4 (CMake target move, NormalUtils normals). [colorsplit] 6 cases / 67 assertions, [paintdepth] 94 green. Review dispatched (sonnet) on b7b8404be6..7f48e2b8a4.
- Task 2: review clean (spec ✅, quality Approved). Minor (deferred): (1) test_color_split.cpp:103-105 facet_state check is tautological → folded into Task 3 as a geometry-correlated assertion; (2) e8a6fa11d7's regression test exercised the local workaround at that commit (bisectability nit) — no action; (3) ColorSplit.cpp:77-80 nozzle lookup underflows when nozzle_diameter is empty → folded into Task 3 (throw ColorSplitError); (4) empty `filaments` untested → folded into Task 3 (throw ColorSplitError on empty list + test).
- Task 2: complete (commits b7b8404be6..7f48e2b8a4, review clean)
- BASE for Task 3 = ae75c30710; Task 3 dispatched (opus) with the three folded T2 minors.
- Task 3 implementer DONE_WITH_CONCERNS (48ae71fcd8, dcb077c19f). Ruling 8 (pinch vertices): a genuine pinch (boundary loop visiting a vertex twice) leaves the two wedges' side strips geometrically coincident along a–a′, which CGAL reports as self-intersection even though per-wedge duplication fixes the topology. Decision: nudge every copy (top, ring, bottom) of a pinch vertex by ε = 1e-3 mm along its wedge's inward tangent bisector; the wedges then no longer touch, at the cost of a 1 µm uncovered sliver at the pinch (far below slicer resolution). The implementer's 7-cell self-touching path fixture becomes the pinned test. — Cost if wrong: a hairline of body colour at pinch points.
- Ruling 9 (fold-guard floor): `max(layer_height, d/2)` may exceed the mid-thickness clamp on features thinner than 2·layer_height. Decision: floor(v) = min(layer_height, d0(v)) with d0 the initial compute_vertex_depths value; pinned by a 40×40×0.3 plate test (top depths ≤ 0.148, shell valid). — Cost if wrong: none (tighter invariant).
- Accepted deviations: exact frustum volume 1326.507 mm³ for the block-top shell (plain-cube corner normals are bisectors → slab only 1.5/√3 deep, a coarse-mesh artefact); halving once per vertex per round; unused params member dropped. Implementer resumed for the pre-review fixes.
- Task 3 fix round (pre-review) 325ba1ae3a: Rulings 8/9 applied; expected values corrected with evidence (6×6 grid for the pinch fixture so the rim bevel does not distort the volume; 0.3 mm plate corner d0 = 0.25781). Ruling 10 (spec rev 2.3): a component that still self-intersects at its floor depth is SKIPPED with a warning (filament + size) instead of refusing the whole split — one micro-feature must not block the user; build_color_shells gains an optional `std::vector<std::string> *warnings` out-param (Task 4 passes &result.warnings). Pinned by a fully painted 0.15 mm sphere → zero shells, one warning. Ruling 11: the pinch nudge is capped proportionally, ε = min(1e-3 mm, 0.1 × shortest boundary edge incident to the pinch vertex). — Cost if wrong: tiny painted features print in the body colour (visible but sub-millimetre) instead of failing loudly.
- Task 3 DONE at 9026efb990 (4 commits); review dispatched (opus) on ae75c30710..9026efb990.
- Task 3 review: spec ✅ except one Important plan-mandated finding (progress values exceed 100 — the brief's `states.size()*4` denominator); quality Approved. Ruling 12: the ColorSplitProgress 0..100 contract (spec/header) wins over the plan's formula — fix by counting components first. Fix round 1 also takes three cheap minors (halve_depth early exit; wedge_of.at(); assert ColorShell::state + a depth_override_mm test). Minor (deferred): check_shell cannot detect a CGAL-dropped non-manifold face (a one-line face-count comparison would make the gate independent of the wedge code); nudge cap keyed on boundary edges only (worst case: a false skip with warning); ColorSplit.cpp at 419 lines — Task 5 is the split point.
- Task 3: fix round 1/5 (4 addressed pending re-review, 0 open — progress ticks bounded via pre-counted components; halve_depth early exit; wedge_of.at; state/override tests; commit 1bdd9a1ec4). Scoped re-review dispatched (sonnet) on 9026efb990..1bdd9a1ec4.
- Task 3: re-review — all 4 findings ADDRESSED, no new breakage. Out-of-scope: compute_vertex_depths can go negative when t(v) < 0.004 mm (t/2 − δ < 0), which the fold guard's orientation test cannot catch for parallel offsets → folded into Task 4 as a clamp d(v) ≥ 0 with a test.
- Task 3: complete (commits ae75c30710..1bdd9a1ec4, review clean)
- BASE for Task 4 = 9562e9abfd; Task 4 dispatched (opus) — spike checkpoint task.
- Task 4 implementer DONE_WITH_CONCERNS (7d4f0d8f66). SPIKE VERDICT: engine A as designed — 99,224-tri sphere: 0.89 s (1 colour) / 3.82 s (3 colours); Manifold Split 76 %, CGAL check 11 %; no fixture hit the self-intersection fallback. Two real defects found and fixed by the implementer: Manifold emits zero-length runs (island absorption would never have fired), and the d >= 0 clamp.
- Ruling 13 (S1 boss, spec 3.1 addendum, rev 2.4): the boss piece was a cup (8.29 of the 9.43 mm3 exposed boss) because a two-ring cylinder has NO interior side vertices — every side vertex is a 45-degree junction bisector, so no offset is radial and the hollow core opens into the block (not an enclosed island). This is the common STL case (cylinders/pins/bosses have two rings). Decision: add a flat refinement pre-pass — F' = Manifold(F).RefineToLength(L) with L = max(ws, min(D_eff, bbox_diag/20)), states mapped by nearest original facet (centroid, AABB on F) — before normals/shells. Cost: more triangles only where edges exceed L. The brief's pi*4 expectation was also wrong (1 mm of the boss is buried; exposed volume = pi*3 = 9.425). New acceptance: piece >= 0.9 x 9.425 after refinement + Ruling 14.
- Ruling 14 (spec 3.6 rev 2.4, concave creases): at a boundary whose outside neighbour is CONCAVE relative to the painted face (n_Q . t_in > 0, e.g. a painted boss side meeting the block top) the wall follows the painted face's own normal, a' = a - d*n_P, with no step and no bisector — otherwise the bisector carries the piece into the neighbouring body (a hidden painted skirt inside the block under the boss rim costs toolchanges on layers that have no paint). Always on, independent of the crease_step flag.
- Ruling 15: ColorSplit.cpp (610 lines) is split in the next task into ColorSplit.cpp (API/pipeline), ColorSplitShell.cpp (groups + ShellBuilder), ColorSplitPartition.cpp (Manifold), ColorSplitInternal.hpp; the unreachable "shell depth reduced" warning branch is removed; the 0.003 mm plate behaviour (probe discards hits < 5 um -> full D) is accepted as physically irrelevant.
- Plan restructured: NEW Task 5 = refinement pre-pass + concave-crease rule + file split (re-measure S1/S3); old Tasks 5-9 renumbered 6-10. Task 4 review dispatched (opus) on 9562e9abfd..7d4f0d8f66 with the interim S1 pin (piece == shell, > 80 % of the exposed boss) accepted for this task.
- Task 4 review: spec ❌ (one Important: the "empty piece dropped with a warning" path is dead — a filament whose shells are fully consumed produces no map entry, so no warning), quality Needs fixes; Manifold header checks all confirmed the implementation (Split order, AsOriginal, runIndex, Cancelled). ⚠️ S2/S4 not measured here: S2 is covered by Task 3's fold-guard/plate/sphere tests, S4 is Task 8 — resolved. Fix round 1 takes the Important plus the cheap minors (abs volume tolerance, single GetMeshGL64 export, tolerance comment, effective_depths helper, multi-shell island test, cancellation test above 50, ctx.FromMeshGL ingest, tautological duration assert). Deferred minor: partition_by_shells leaves result.depths default (only split_volume_by_paint fills it) — documented, not fixed.
- Task 4: fix round 1/5 (Important + 8 minors addressed pending re-review; commit f44db8be2d). Scoped re-review dispatched (sonnet).
- Task 4: re-review — all 9 findings ADDRESSED, no new breakage. Minor (deferred): the island-attribution test's `body < 1e-6` bound would also pass with absorption disabled (the island is tiny by construction) — strengthen in the final review pass if cheap; partition_by_shells leaves result.depths default.
- Task 4: complete (commits 9562e9abfd..f44db8be2d, review clean)
