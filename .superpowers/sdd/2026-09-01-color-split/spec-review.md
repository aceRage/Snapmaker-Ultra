# Adversarial review of the colour-split spec rev 1 (2026-09-01) — condensed

Reviewer: subagent, read-only, against C:\Dev\SnapmakerOrcaNext and the Manifold 3.5.2 headers. All items below were folded into spec rev 2 unless marked "open".

## Critical
- C1 Shell validity: vertex-normal offsets invert at CONVEX features with radius < d (fillets, bosses, text) and swallowtail at concave creases; "Unlimited = bbox diagonal" guaranteed inversion; Manifold Status() checks topology only (mesh.h:43-46; manifold.h:124-140 has no self-intersection error; common.h:145/153 ε-valid input; selfIntersectionChecks debug-only common.h:664-666); the volume check would not catch folded shells. FIX (rev 2 §3.4/3.8): d(v)=min(D, t(v)/2) via AABBMesh ray thickness; fold guard; cgal::does_self_intersect per shell (MeshBoolean.cpp:460); Unlimited = the clamp alone; enclosed body islands absorbed via runOriginalID provenance; spike fixtures must include convex features and a fully painted closed patch.

## Important
- I1 Pinch boundaries (vertex with >2 boundary edges) → 4 faces on one edge → NotManifold. FIX: duplicate per wedge (Merge() merges only along open edges, mesh.h:167-173).
- I2 Cap angle: live 2D rule is morphological (flat_cap_component_ex MultiMaterialSegmentation.cpp:1493-1578): capped iff run > 3·ws ⇔ tanθ < h/(3ws) ≈ 2.17° at 0.1 mm; 6.49° is the retired N1 gate (:1601-1606); tests :5107-5200 require 3/4/5° uncapped. ws = outer_wall_line_width vs nozzle_diameter[0] + spacing at the layer height, max over regions (:1786-1797). FIX adopted.
- I3 Cap depth: effective_shell_layers_by_thickness (:1381-1420, :1777-1780) = N layers or layers spanning T; 0 when N == 0 (thickness dead); gates normal_shell (D ≥ ws, :1874-1877) and D deeper than shell (:2228); classify by facet n_z sign (:1985-1993). FIX: d_cap = max(h, N_eff·h), gates mirrored.
- I4 (other, mesh&&) ModelVolume ctor is PRIVATE (Model.hpp:1037, :1134-1136; friends Model/ModelObject :1026-1027); public add_volume(const ModelVolume&, TriangleMesh&&) appends + centres unconditionally; m_is_splittable cache (Model.hpp:1060) stale after set_mesh. FIX: all outputs are new volumes via add_volume; source deleted; volumes rotated into place.
- I5 New-mesh ctor copies text_configuration, emboss_shape, cut_info (Model.hpp:1135-1136); EmbossJob would regenerate (EmbossJob.cpp:418-430). FIX: reset all three; name exterior_facets in resets.
- I6 Empty body unhandled (thin plate painted both sides, fully painted part). FIX: delete source, first piece first.
- I7 center_geometry_after_creation composes wrongly with rotation/scale (Model.cpp:2681-2697; ModelVolume::split :2872-2876 and GLGizmoMeshBoolean :486 share the defect). FIX: set_offset(src.offset + src.matrix_no_offset * init_shift). its_transform has no fix_left_handed (that is TriangleMesh::transform, TriangleMesh.hpp:115). Split in volume space with D/s when the scale is isotropic.
- I8 Job safety: nothing blocks painting/undo/delete during a UI job (Plater.cpp:17311-17344); MMU gizmo re-reads only on object id/volume count change (GLGizmoPainterBase.cpp:41-51) → stale selector writes onto the new mesh (GLGizmoMmuSegmentation.cpp:971). FIX: ids + paint timestamp re-check in finalize; refuse while a painter gizmo is open (GUI_ObjectList.cpp:2554-2577 pattern); guard replace_job with is_idle (Plater.cpp:16884-16887).
- I9 paint_infill_override=false not mirrored (per-volume extruder sets sparse too, PrintObject.cpp:3249-3256; 2D keeps base sparse, PrintApply.cpp:1096-1103). FIX: dialog option → sparse_infill_filament = body extruder on colour parts.
- I10 Bisector taper leaves a zero-width body wedge at a painted-top/side-face edge; 2D holds inferred claims one wall stack off the contour (F1 inset :2323) and absorbs slivers; slices_to_regions has no such cleanup (PrintObjectSlice.cpp:420-437). FIX: wall-stack step (§3.6) + S4 measurement.
- I11 Use Manifold::Split (manifold.h:232) sequentially instead of unions of touching solids; adjacent equal-depth patches have COINCIDENT side walls, not overlaps. FIX adopted (§3.8).
- I12 Depth inputs derivable without PrintObject: Flow::new_from_config_width (Flow.hpp:108, Flow.cpp:111-125); PrintRegion::flow resolution (PrintRegion.cpp:72-101); keys PrintObjectConfig layer_height :917, interface_shells :916, paint_depth_* :925-927, paint_infill_override :932, paint_depth_solid_interfaces :943, wall_generator :1023; PrintRegionConfig outer/inner_wall_line_width :1095/:1150, top/bottom shell :1164-1165/:1080-1081; PrintConfig nozzle_diameter :1518; GUI source wxGetApp().preset_bundle->full_config() (Plater.cpp:13562) ⊕ object ⊕ volume config (PrintObject.cpp:3275-3290). FIX adopted (§3.3).

## Minor
- M1 FacetsAnnotation::reset keeps used_states (Model.cpp:3618-3623) — moot with new volumes. M2 body extruder key only if the source had it (outer_wall_filament reset PrintObject.cpp:3257-3259). M3 states above filament count / mixed virtual ids (gizmo clamps GLGizmoMmuSegmentation.cpp:1035-1036; PrintObject.cpp:3294-3299 clamp) — rule in §3.1, verification OPEN for the plan. M4 AngleWeighted normals (NormalUtils.hpp:20-24). M5 ObjectList::add_volumes_to_object_in_list public (GUI_ObjectList.hpp:340); drop Plater::update duplication; plain part menu single Split item (GUI_Factories.cpp:1540-1549). M6 MeshGL64 + explicit tolerance (mesh.h:159-163), Manifold::Volume(), defined tol. M7 ExecutionContext cancel (common.h:190-280). M8 test-5 tolerance defined (vertical-wall parity only). M9 public depth helper; BBS backup via add_volume. M10 consistency items fixed.

## Manifold headers promise (verbatim scope)
Input = oriented 2-manifold triangle mesh (manifold.h:45-47); Status errors (:124-140); Merge best-effort along open edges (mesh.h:167-181); tolerance semantics (mesh.h:159-163; SetTolerance :214); booleans deferred until Status/eval (:148-171); Split/Decompose/NumDegenerateTris/MatchesTriNormals/Minkowski declared (:232,:95,:322,:321,:236-237) semantics unstated; per-object mutex + atomic ctx (:339-357) → off-thread use fine; SILENT on self-intersecting input.

## Solid (confirmed by the reviewer)
Exact tiling formulas; one-selector patch extraction; welding preserves face order; shell closure for manifold-with-boundary components (side strip orientation (b,a,a′,b′)); slicing consequences (own perimeters Layer.cpp:179-184; later wins PrintObjectSlice.cpp:420-437; interface_shells PrintObject.cpp:1337-1338,1772-1773; flush ToolOrdering.cpp:1844-1856; Ultra outer_wall_filament reset); Print::apply via model_volume_list_changed (Model.cpp:3710-3745; PrintApply.cpp:1605-1612); config.set("extruder", int) (Config.hpp:2266); PaintDepth helpers (PaintDepth.hpp:105-107,149); Job scaffolding (PlaterWorker.hpp:65,88-93; Plater.hpp:435).
